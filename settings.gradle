pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

String storageUrl = System.env.FLUTTER_STORAGE_BASE_URL ?: "https://storage.googleapis.com"

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        maven {
            url '/Users/dimuthu/EpicLanka/Projects/seylan_ekyc_digi_face/build/host/outputs/repo'
        }
        maven {
            url "$storageUrl/download.flutter.io"
        }
        maven {
            url 'https://www.jitpack.io'
        }
    }
}
rootProject.name = "SDK Added Compose"
include ':app'
